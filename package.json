{
  "name": "intern-task-monorepo-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd task-manager/frontend && npm install",
    "build": "cd task-manager/frontend && npm run build",
    "start": "cd task-manager/frontend && npm run dev"
  },
  "engines": {
    "node": ">=18"
  }
}
